/** Marks a driver or van that was typed in per trip rather than read off a roster. */
export function RentalTag() {
  return (
    <span className="w-fit flex-none rounded-pill bg-brand-tint px-2 py-0.5 text-xs font-semibold text-brand">
      Rental
    </span>
  );
}
